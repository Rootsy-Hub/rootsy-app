/** Brillo mínimo — macOS flat, sin capas de gel. */
export function MenuIconChrome() {
  return (
    <div className="pointer-events-none absolute inset-0 bg-gradient-to-b from-white/10 to-transparent" />
  )
}
